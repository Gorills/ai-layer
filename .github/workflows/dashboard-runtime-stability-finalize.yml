name: dashboard-runtime-stability-finalizer

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  dormant:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Finalizer is intentionally dormant while source changes are staged.'
