name: lint-cleanup-once
on:
  push:
    branches:
      - audit/agent-facing-contracts
permissions:
  contents: write
jobs:
  cleanup:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: audit/agent-facing-contracts
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install ruff==0.16.2
      - run: python scripts/lint_cleanup_once.py
      - run: ruff check --fix src tests scripts
      - run: ruff format src tests scripts
      - run: rm -f scripts/lint_cleanup_once.py .github/workflows/lint-cleanup-once.yml
      - name: Commit cleanup
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git diff --cached --exit-code && exit 0
          git commit -m 'style: normalize audited agent contracts'
          git push origin HEAD:audit/agent-facing-contracts
